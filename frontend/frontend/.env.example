# Copy this file to .env and replace the IP with your machine's local WiFi IP.
# Find it with: ipconfig (Windows) or ifconfig (Mac/Linux) — look for IPv4 address.
# The IP must be reachable from the phone/emulator on the same WiFi network.
EXPO_PUBLIC_API_BASE=http://YOUR_LOCAL_IP:8000/api
